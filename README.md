# angular_ui_grid_demo

small angular ui-grid demo, with features: sort, hide columns, resize columns (by their headers), and drag&drop (by headers either). 
last two features are not stable due to docs
